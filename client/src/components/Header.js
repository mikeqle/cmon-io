import React from 'react';
import NavBar from './NavBar';

const Header = (props) => {
    return (
        <div>
            <h1>c'mon</h1>
            <p>A common set of facts</p>
            <NavBar />
        </div>
    )
};

export default Header;